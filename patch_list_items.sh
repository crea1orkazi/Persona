sed -i 's/itemsIndexed(messages, key = { _, msg -> msg.id }) { index, message ->/val reversedMessages = messages.reversed()\n                    if (isTyping) {\n                        item(key = "typing") {\n                            TypingIndicatorBubble(\n                                role = if (isGroupChat) "AI" else character?.name ?: "AI",\n                                avatarUri = if (isGroupChat) null else character?.avatarUri,\n                                modifier = Modifier.animateItem()\n                            )\n                        }\n                    }\n                    itemsIndexed(reversedMessages, key = { _, msg -> msg.id }) { index, message ->/' app/src/main/java/com/example/ui/screens/GroupChatScreen.kt
